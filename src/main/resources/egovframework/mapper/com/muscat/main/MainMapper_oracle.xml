<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="egovframework.com.muscat.main.mapper.MainMapper">

	<select id="selectBssId" resultType="string">
		SELECT BBS_ID
		FROM COMTNBBSMASTER
		WHERE CATEGORY_MAIN = 'BB100'
		AND CATEGORY_SCOPE = 'BT100'
		AND USE_AT = 'Y'
	</select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="egovframework.com.muscat.admin.mapper.UserMapper">

	<select id="checkUserAuth" resultType="int" parameterType="String">
		SELECT COUNT(*) FROM COMTNEMPLYRSCRTYESTBS
		WHERE SCRTY_DTRMN_TRGET_ID = #{uniqId}
	</select>

</mapper>
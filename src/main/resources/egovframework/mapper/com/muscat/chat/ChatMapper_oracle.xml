<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="egovframework.com.muscat.chat.mapper.RoomMapper">
	
	<!-- 조회 -->
	<select id="findroom" parameterType="UserVO" resultType="roomVO">
	select *
	from room r join users u on r.room_id = u.room_id
	where user_id = #{userId} and status = 'Y'
	</select>
	
	<!--  채팅방 인원 조회 -->
	<select id="findParticipantsByRoomId" parameterType="String" resultType="String">
	SELECT u.user_id
	FROM users u
	JOIN room r ON u.room_id = r.room_id
	WHERE u.room_id = #{roomId}
    </select>
	
	
	<!-- 방등록 -->
	<insert id="roomi" parameterType="roomVO">
	<selectKey keyProperty="roomId" resultType="String" order="BEFORE">
		SELECT NVL(MAX(to_number(room_id)),0)+1 AS room_id  
		FROM room
	</selectKey>
	insert into room (room_id, room_name, mg_date)
 	values (#{roomId}, #{roomName},sysdate)
	</insert>
	
	<!-- 사용자 등록 -->
	<insert id="insertUser" parameterType="userVO">
	insert into users (user_id, room_id, last_channel_info, status )
	values (#{userId}, #{roomId},null, 'Y') 
	</insert>
	
	
	<!--  메시지 등록 -->
	<insert id="insertMessage" parameterType="MessageVO">
	insert into Message (message_id, room_id, mg_file_info, mg_count_num, mg_photo, mg_type, user_id, mg_content )
	values (#{messageId},#{roomId},#{mgFileInfo},#{mgCountNum},#{mgPhoto},#{mgType},#{userId},#{mgContent})
	</insert>
	
	
	
</mapper>